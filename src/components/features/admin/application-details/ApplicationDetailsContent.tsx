import { FunctionComponent } from "react";
import { PageContent } from "../../layouts/PageContent";

export const ApplicationDetailsContent: FunctionComponent = () => {
  return (
    <PageContent
      title="Application Data"
      subtitle="The quick brown fox jumps over a lazy dog near the riverbanks."
      content={<></>}
    />
  );
};
